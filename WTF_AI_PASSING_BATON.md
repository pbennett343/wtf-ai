# WTF AI Passing Baton

## 🎯 GOAL & PROJECT OUTLINE
**Goal:** Automate the creation of high-quality "WTF Stats" graphics for IG/X. The final output must be exactly 4000x5333 pixels, imitating the original WTF Stats style (fonts, layout, colors) perfectly. The long-term vision is full multi-platform automation, but currently relies on the GM (Grandmaster) to guide the training.
**Outline:** The app acts as a 6-step tool:
1. Stat Input (Manual text for MVP)
2. Text Editor (Context, visual spacing)
3. Photo Upload
4. Table Upload (Optional)
5. Visual Controls (Zooming, moving)
6. Export (Render DOM to 4000x5333 PNG)

## 🚨 STRICT PROTOCOL (PERMANENT INK - NEVER DELETE)
1. **REPEAT & CLARIFY BEFORE ACTING:** ALWAYS respond back simply repeating what the user asked you to do. Explain your proposed logic/why and *ask for confirmation* before changing code. 
2. **VERCEL PREVIEW PUSHES:** NEVER push to the `main` branch. The GM has limits on production deployments. ALWAYS explicitly checkout and push everything exclusively to the `preview` branch (`git push origin preview`). You do not need GM's permission to push to the preview branch, keep continually pushing your code iterations there.
3. **JOURNALING:** This document must be updated continuously. Treat it like permanent ink. Add new dates/entries to the bottom capturing what was built, what broke, and what the next agent needs to know. Do NOT delete old entries.
4. **STYLE TRAINING:** Keep adding to the `pbs_script.md` artifact to learn and mimic the exact phrasing/spacing stylistic choices of the Grandmaster over time.
5. **BRAND HEADER LOCK:** The top logo/header generation inside the Next.js `page.tsx` canvas utilizes raw, external static snapshots via `<img>` tag (e.g. `wtf-x-logo.jpg`). NEVER, under any circumstance, attempt to recreate these headers using HTML/CSS or React icons. The Brand Select dropdown seamlessly updates the `img` path. This mechanism is permanently locked.
6. **NO BROWSER VERIFICATION:** NEVER open the browser to check Vercel preview deployments. The GM verifies all previews. Push code to the `preview` branch and report what was changed — the GM handles all visual QA. This avoids excessive screen recording data and wasted compute.

---

### Journal Log
*   **(April 19, 2026)** - Initial project setup. Created scaffolding for Next.js MVP focusing on the 6-step wizard and 4000x5333 output requirement.
*   **(April 19, 2026)** - [Vercel Build Fix] Missing `autoprefixer` module caused remote build to fail. Hand-injected into package.json and pushed alongside the core 6-step MVP UI skeleton.
*   **(April 19, 2026)** - [AI & JPG Update] Integrated Gemini AI for image scanning and "WTF Style" text rewording. Switched final output to JPG (4000x5333). Added API validation warning and loading spinners for all AI actions.
*   **(April 21, 2026)** - [Code Audit & Theme Overhaul] Full codebase audit: fixed 5 bugs (state declaration ordering, genAI module-scope init in reword route, hardcoded MIME type in vision route, GraphicTemplate remounting). Added Photo Upload Toggle (raw vs AI generate) in Step 3. Complete WTF Sports brand theme overhaul — navy (#3b3b6d) and crimson (#b42434) across entire UI: sidebar, nav, buttons, sliders, scrollbar, inputs, preview stage. Updated Tailwind config with brand color tokens.
*   **(April 21, 2026)** - [Clay Figurine Style] Rewrote generate API prompt to produce 3D clay figurine style images — faceless smooth matte clay figures with blank oval heads, realistic proportions, team-colored uniforms. Embedded full 32-team NFL color reference dataset (hex codes from GM's Google Sheet) directly into the route. Reference image upload also supported for richer context.
*   **(May 9, 2026)** - [Icon Fix] Generated padded square `icon.png` and `apple-icon.png` using WTF Sports logo on navy `#3b3b6d` background to prevent stretching on iPhone home screen. Committed to `src/app/`.
*   **(May 9, 2026)** - [Mobile UX + Step 4 Removal + Step 3 AI-Only] Removed Step 4 (Add Table) from nav and rendering. Overhauled Step 3: removed the raw/AI toggle, Step 3 is now exclusively the AI clay generator (upload a reference image and/or type a prompt → "GENERATE CLAY MODEL"). Fixed mobile layout: outer wrapper changed from `h-screen overflow-hidden` to `min-h-screen overflow-y-auto`, preview sits at top on mobile (`order-1`), controls scroll below (`order-2`). Fixed build errors from a bad partial edit: removed unused `Table2`, `ToggleLeft`, `ToggleRight` imports; removed unused `photoMode` state; fixed `Step` type union to exclude `4`.
*   **(May 9, 2026)** - [Find Stats & AI Updates] Integrated a Google Search grounded "Find Stats" feature on Step 1. The API pulls historical/anomalous stats directly from X (@wtfstats, @optastats, etc) and OddsShark. Reverted Find Stats prompt to ensure accurate historical data vs predictive data. Upgraded Step 3 Image Generator to use a two-step process to correctly identify a player's team and accurate uniform via search, rendering them on a heavily blurred, context-aware field/arena backdrop. Refined UI with absolute positioned mobile header, drag/drop/paste support for images, and UI dates/sources for generated stats.
*   **(May 10, 2026)** - [Mobile Layout + Brand Home + Export] Fixed mobile layout: outer wrapper is now `h-screen overflow-hidden`, preview is locked at `h-[50vh]` (top), sidebar is `h-[50vh]` (bottom) with `overflow-y-auto flex-1` inner scroll — controls scroll within themselves, preview never moves. Added Step 0 brand home screen (shown before Step 1) with brand card tiles that set the brand and advance to Step 1. Nav tabs hidden on Step 0. Added `textBottomPadding` slider in Step 2. Rewrote `handleExport` to use Web Share API (`navigator.share` with a `File`) on iOS — gives native "Save Image" sheet to Camera Roll. Falls back to standard `<a>` download on desktop.
*   **(May 25, 2026)** - [WTF Style Button Removal] Removed the "WTF Style" button from Step 1 options inside the main workspace component (`src/app/page.tsx`). The underlying API route (`/api/ai/reword`) and the `handleAIReword` state logic are kept intact in the codebase in case we need to re-enable them later. Pushed cleanly to the `preview` branch.
*   **(May 25, 2026)** - [Matchup Overlay Card] Designed and integrated a gorgeous, translucent matchup overlay card within the high-res 4000x5333 visual canvas (`GraphicTemplate`). Renders game meta info, team names/logos (linked to ESPN scoreboard CDN), current records, and Moneyline and Runline odds. Added full step-by-step custom inputs and MLB team quick-select dropdown configurations in the Step 5 "Visuals" control panel. Pushed cleanly to the `preview` branch.
*   **(May 25, 2026)** - [Auto-Zoom + Matchup Auto-Fill] Two major enhancements: (1) "APPLY TO GRAPHIC" button now auto-defaults Photo Zoom to 188% and Vertical Pan to 688px so AI-generated clay images always frame with correct headspace. (2) Created `/api/ai/matchup-search` route powered by Gemini 2.5 Flash with Google Search grounding — when a stat card is selected in WTF Bets mode, the system automatically queries Google for the team's next game time (EST), opponent, both teams' records, and current Moneyline/Runline odds, then auto-populates the matchup overlay card. Added an "Auto" manual trigger button in the Step 4 Visuals panel and a spinning loading indicator during search. Pushed cleanly to the `preview` branch.
*   **(May 26, 2026)** - [JSX Syntax Fix + WTF Bets Finder Fix + Name Wrapping Polish] Resolved JSX syntax error in `page.tsx` that broke the Vercel build, caused by a misplaced fragment close tag `</>` and missing bracket inside the Matchup Overlay controls panel. Fixed a crash in the `/api/ai/find-stats` API route when running in WTF Bets mode due to a schema response type mismatch (Gemini does not support responseSchema / responseMimeType when Google Search grounding tools are active). Reverted the model config to regex-based JSON extraction, and optimized search performance by enforcing a FAST EXECUTION BUDGET of max 2 queries in the prompt to prevent Vercel 10s serverless timeouts. Removed `truncate` styling from team names (Matchup Overlay) and player names (Stat Context Card) to allow them to wrap naturally with `leading-none` and `break-words`. Confirmed full brace and bracket validation. Pushed to `preview`.
*   **(May 26, 2026)** - [Context Chart Editor + Workspace Clear] Added layout customization to the Stat Context Chart in Step 5, including `Scale` (50% to 150%) and `Bottom Offset` sliders. Built a raw JSON editor (`isEditingStatContext`) that allows manual overriding of chart columns, rows, or values directly within the UI, alongside a "LOCK DATA" toggle to prevent auto-regeneration when selecting new stats. Added "Clear Workspace" buttons to Step 1 and Step 3 to fully reset app state (text, photos, overlays) without reloading the window. Finally, adjusted the `WTF Stats` brand configuration to default to `1.45` line height. Pushed to `preview`.
*   **(May 31, 2026)** - [Giveaway Tool Integration] Integrated the WTF Giveaway tool directly into the Next.js app so it can run remotely on Vercel without a local Python backend. Added a "WTF Giveaway" link to the Step 0 Brand Home. Created `/giveaway` route mimicking the VFL Data Wizard, allowing users to paste text or upload Images/Videos (using `VideoFrameExtractor` ported from VFL Hub). Built `/api/ai/giveaway-scanner` using Gemini 1.5 Pro to extract usernames based on a custom "Winning Answer" and a misspelling toggle. Rebuilt the python `MoviePy` spinning wheel generator entirely in React/HTML5 Canvas (`WheelGenerator`) to spin, animate, display confetti, and auto-record a downloadable `.mp4` file via the `MediaRecorder` API directly in the browser. Pushed to `preview`.
*   **(May 31, 2026)** - [Giveaway Wheel & Scanner Polish] Perfected the canvas-based wheel generator (`WheelGenerator.tsx`): corrected the canvas rotation direction from counter-clockwise to clockwise (`ctx.rotate(angle)`) so the stopping physics land precisely on the announced winner; loaded and integrated the flat `/wtf-logo-transparent.png` logo in the top-right corner; refactored the random flicker confetti into a smooth falling loop particle system cascading at half-speed; synchronized the falling confetti and the congrats winner card to appear simultaneously the exact moment the wheel stops; and extended the celebration phase back to 4 seconds (12s total video duration). Additionally, optimized the scan page (`giveaway/page.tsx`) to incrementally populate the names list in real-time as batches resolve, and isolated each batch call in its own try-catch block to guarantee that network glitches or timeouts do not abort the entire scan. Pushed cleanly to `preview`.
*   **(June 15, 2026)** - [Giveaway Standings & Solid Black Video Background] Implemented "WTF Giveaway Games" standings tracking system. Added a toggle to settings to opt-in to stats tracking, alongside a prize amount input field. When the wheel completes spinning, candidate PWA, W, L, Win %, and Total Winnings are updated in localStorage. Seeding defaults exactly match the spreadsheet screenshot provided by the GM (ranks 1 to 26, total 36 giveaways, and $1,093.80 in winnings). Added a high-fidelity modal view accessible at any point containing the standings, competition ranking ties, import/export copy-paste spreadsheet parser, and clear/reset controls. Changed the background color of the `WheelGenerator` canvas from `#111114` to `#000000` to guarantee a solid black video background without color shifts during celebration overlays. Pushed cleanly to `preview`.
*   **(July 24, 2026)** - [Book Illustrations Mode & Transparent PNG Export] Added a new "Book Illustrations" mode tile on the Step 0 Home screen brand grid. Integrated a simplified black ink sketch illustration prompt in `/api/ai/generate/route.ts` powered by Gemini image generation. In Book Illustrations mode, the prompt is implied directly from the entered stat text. Built a client-side HTML5 canvas alpha-keying pass (`removeWhiteBackground`) that converts all white/light backdrop pixels (`RGB > 215`) to `rgba(0,0,0,0)`, producing a true 100% transparent PNG sketch. Added a dedicated "DOWNLOAD TRANSPARENT PNG" button in Step 3 for direct PNG export ready for book publishing. Enhanced prompt analyzer in `/api/ai/generate/route.ts` to extract both the athlete AND the specific dramatic event/action (e.g. lightning strike, dunk, diving catch). Enforced strict legal trademark sanitization (unbranded jerseys, plain caps with no team letters or logos) and prohibited all rendered text/dates/numbers from appearing in generated book sketches. Pushed cleanly to the `preview` branch.




